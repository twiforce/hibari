doctype html
html(lang="ru")
  head
    include head
    mixin head()
  body
    #wrap
      nav.navbar.navbar-inverse.navbar-fixed-top(role="navigation")
        include nav
        mixin navheader()
        #nav-collapsible.collapse.navbar-collapse
          ul.nav.navbar-nav
            mixin navdefaultlinks("/")
          mixin navloginlogout("/")
      section#mainpage
        .container
          .col-lg-8.col-md-8
            h1 Добро пожаловать. Снова.
            p SynchTube - место для совместного просмотра роликов Youtube, прямых эфиров Justin.tv и <a href="help.html" rel="tooltip" data-toggle="tooltip" data-placement="top" title="" data-original-title="Vimeo, Dailymotion, Soundcloud, Ustream, Livestream и даже свой RTMP-поток через JWPlayer">многого другого</a>. Зарегистрируйте комнату, настройте ее по своему вкусу, позовите друзей и начните просмотр - вместе.
            h3 Открытые комнаты
            table.table.table-bordered.table-striped
              thead
                th Комната
                th Онлайн
                th Воспроизводится
              tbody
                each chan in channels
                  tr
                    td: a(href="/r/#{chan.name}") #{chan.pagetitle} (#{chan.name})
                    td= chan.usercount
                    td= chan.mediatitle
          .col-lg-4.col-md-4
            h3 Зайти в комнату
            input#channelname.form-control(type="text", placeholder="Название комнаты")
            p.text-muted Зарегистрировать свою комнату можно <a href="/account/channels">здесь</a>.
    include footer
    mixin footer()
    script(type="text/javascript").
      $("#channelname").keydown(function (ev) {
        if (ev.keyCode === 13) {
          location.href = "/r/" + $("#channelname").val();
        }
      });
